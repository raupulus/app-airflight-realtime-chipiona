import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'es.desdechipiona.aviones',
  appName: 'Vuelos en Chipiona',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
